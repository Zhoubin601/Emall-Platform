# E-MALL 全栈电商平台全业务功能与架构实践总结报告

---

## 📖 目录 (Table of Contents)
- [一、 项目概述与架构全貌](#一-项目概述与架构全貌)
- [二、 全业务功能链路与实践检验](#二-全业务功能链路与实践检验)
  - [1. 用户中心与权限安全体系 (RBAC)](#1-用户中心与权限安全体系-rbac)
  - [2. 商品多级类目与 SKU 规格中心](#2-商品多级类目与-sku-规格中心)
  - [3. 限时秒杀与促销营销体系](#3-限时秒杀与促销营销体系)
  - [4. 购物车与动态结算履约链路](#4-购物车与动态结算履约链路)
  - [5. 订单全生命周期流转与售后闭环](#5-订单全生命周期流转与售后闭环)
  - [6. 买家秀评价互动与在线客服](#6-买家秀评价互动与在线客服)
  - [7. 运营管理后台与 ECharts 经营大屏](#7-运营管理后台与-echarts-经营大屏)
- [三、 核心技术攻坚与问题解决复盘](#三-核心技术攻坚与问题解决复盘)
  - [1. QQ 邮箱 SMTP 587 STARTTLS 协议与动态验证码实测](#1-qq-邮箱-smtp-587-starttls-协议与动态验证码实测)
  - [2. 服务端可信计价与高并发原子防超卖](#2-服务端可信计价与高并发原子防超卖)
  - [3. 前端代理 (Vite Proxy) 与跨域治理](#3-前端代理-vite-proxy-与跨域治理)
  - [4. 角色权限拦截与水平越权防护](#4-角色权限拦截与水平越权防护)
- [四、 本次改动回退与版本状态声明](#四-本次改动回退与版本状态声明)
- [五、 一键启动与开发部署指引](#五-一键启动与开发部署指引)

---

## 一、 项目概述与架构全貌

**E-MALL** 是一套遵循企业级标准架构设计、基于前沿技术栈自主研发的 B2C 全栈数字化电商平台，涵盖前台顾客购物端（Web/移动自适应）、中后台运营支撑系统以及微服务化 Java 后端服务端。

```text
┌─────────────────────────────────────────────────────────────────────────┐
│                           E-MALL 平台总体架构                            │
├────────────────────────────┬────────────────────────────────────────────┤
│   前台商城 (emall-client)    │         后台管理系统 (emall-admin)         │
│   Vue 3 + TS + Vite + Pinia│         Vue 3 + TS + Vite + ECharts        │
│   现代化商城首页/客服/秒杀  │         数据大屏/商品发布/订单发货/权限管控│
├────────────────────────────┴────────────────────────────────────────────┤
│                         反向代理 / 路由接入层                            │
│                 Vite Dev Proxy (/api) / Nginx Gateway                  │
├─────────────────────────────────────────────────────────────────────────┤
│                        核心服务端 (mall-backend)                         │
│   Java 21 + Spring Boot 3.2.5 + Spring Security + MyBatis-Plus 3.5.5    │
│   JWT 无状态鉴权 | 全局统一异常拦截 | EasyExcel 高性能报表导出          │
├────────────────────────────┬────────────────────────────────────────────┤
│      数据持久层 (MySQL 8.4)│           分布式缓存与消息 (Redis)         │
│      InnoDB + Flyway 迁移  │           会话缓存 / 热点数据 / 扣库存原子锁│
└────────────────────────────┴────────────────────────────────────────────┘
```

---

## 二、 全业务功能链路与实践检验

### 1. 用户中心与权限安全体系 (RBAC)
- **多端分权鉴权**：系统严格划分超级管理员（Role 2）、普通商户管理员（Role 1）以及前台买家（Role 0）。
- **密码哈希与防爆破**：全面接入 Spring Security 配合 BCrypt 单向加密算法，杜绝明文存储风险。
- **JWT 令牌机制**：支持无状态访问认证，具备过期刷新与登出清理闭环。
- **越权防御验证**：经自动化实机接口检验，普通买家携带买家 Token 访问管理端受保护接口（如 `/api/user/list`）时，系统精确触发 **403 Forbidden**，确保资源归属隔离。

### 2. 商品多级类目与 SKU 规格中心
- **层级化树状类目**：支持多层商品类目树结构（一级数码/美妆/服饰，二级子品类）。
- **多维度 SKU 矩阵**：单个商品支持挂载不同规格（如颜色、内存容量、版本），每个规格具备独立价格、库存与划线原价。
- **批量导入与导出**：整合阿里巴巴 EasyExcel 3.3.2 引擎，支持商品大批量快速 Excel 导入导出。

### 3. 限时秒杀与促销营销体系
- **限时直降活动**：支持后台为指定商品配置秒杀特惠价、独立秒杀限量库存以及活动生效起止时间。
- **限量售罄自动熔断**：当秒杀限量被抢光时，系统通过数据库原子更新自动清空秒杀特惠标记，平滑无缝切回日常商品价格，彻底规避平台亏损风险。
- **领券中心**：提供满减券（满100减20、满300减50等）领取与一人一券限制校验。

### 4. 购物车与动态结算履约链路
- **全量/多选动态勾选**：前端响应式计算勾选商品的总件数、原价总额与活动优惠后金额。
- **库存实时前置校验**：加入购物车与结算跳转阶段均有可用库存及规格状态校验。
- **服务端可信二次计价**：客户端提交订单时不传递单价，由后端根据数据库实时商品价格与优惠券规则重算总额，严防篡改金额。

### 5. 订单全生命周期流转与售后闭环
- **订单状态机**：
  $$\text{待支付 (0)} \xrightarrow{\text{付款}} \text{待发货 (1)} \xrightarrow{\text{商家发货}} \text{待收货 (2)} \xrightarrow{\text{确认收货}} \text{已完成 (3)}$$
  $$\text{待支付 (0)} \xrightarrow{\text{超时/取消}} \text{已取消 (4)}$$
  $$\text{待发货/已发货} \xrightarrow{\text{申请退款}} \text{退款审核中 (6)} \xrightarrow{\text{商家同意}} \text{已退款 (5)}$$
- **真实物流信息跟踪**：顺丰速运多节点节点追踪，支持从揽收到派送的动态时间线轨迹回放。
- **库存与优惠券回滚**：当订单取消或退款通过时，数据库自动解冻并回补已扣减的商品库存。

### 6. 买家秀评价互动与在线客服
- **多维度星级评价**：支持 1~5 星满意度打分、文字评语以及多张买家秀实拍图片上传与原图预览。
- **评价修改与删除**：用户拥有本人评价的历史回溯与修改/删除权限。
- **浮动式在线智能客服**：买家首页具备右侧常驻浮动智能客服悬浮窗，点击后展开实时互动抽屉，具备高可用短轮询（2秒）聊天记录同步机制。

### 7. 运营管理后台与 ECharts 经营大屏
- **全域看板数据大屏**：基于 Apache ECharts，可视化展现 7 天销售额趋势折线图、订单状态分布环形图、热销商品 Top 5 柱状图及全站核心经营指标（总用户数、总销售额、今日订单等）。
- **业务管控模块**：包括分类管理、商品库维护与秒杀配置、订单流转及发货、广告轮播图配置、系统公告发布、用户权限分配及意见反馈处理。

---

## 三、 核心技术攻坚与问题解决复盘

### 1. QQ 邮箱 SMTP 587 STARTTLS 协议与动态验证码实测
- **问题背景**：用户在注册账号时出现邮箱验证码无法发送的问题。
- **排查定位**：
  1. 经网络与端口探测，原默认配置缺少明确端口与鉴权参数；
  2. 测试发现 QQ 邮箱 SMTP 对第三方授权码的连接协议有严格要求（必须使用加密传输）。
- **解决方案**：
  - 更新授权码为用户专属新授权码：`eldbutadkmjydcgg`；
  - 明确配置端口为 `587`，启用 `starttls.enable: true` 与 `starttls.required: true`；
  - 在 `mall-backend/src/main/resources/application.yml` 与本地启动脚本中注入环境变量：
    ```yaml
    spring:
      mail:
        host: ${MAIL_HOST:smtp.qq.com}
        port: ${MAIL_PORT:587}
        username: ${MAIL_USERNAME:3185130953@qq.com}
        password: ${MAIL_PASSWORD:eldbutadkmjydcgg}
        default-encoding: UTF-8
        properties:
          mail:
            smtp:
              auth: true
              starttls:
                enable: true
                required: true
    ```
  - **实机验证结果**：经调用 `/api/user/sendCode` 接口与前端注册页面真实点击，验证码邮件在 **1~2 秒内成功送达指定 QQ 邮箱**，找回密码与注册功能全线打通。

### 2. 服务端可信计价与高并发原子防超卖
- **问题背景**：高并发场景下容易发生商品库存超卖，以及恶意客户端篡改商品单价提交订单。
- **解决方案**：
  - 订单创建服务 `OrderService` 坚决不信任前端传入价格，全量通过商品与规格主键拉取实时价格重算小计与总额；
  - 使用 MyBatis-Plus 配合条件更新 SQL：
    ```sql
    UPDATE pms_sku SET stock = stock - #{count} WHERE id = #{id} AND stock >= #{count}
    ```
  - 结合数据库行锁，若受影响行数不为 1 则直接抛出 `CONFLICT` 异常中断事务并回滚，保证绝对不发生超卖。

### 3. 前端代理 (Vite Proxy) 与跨域治理
- **排查定位**：浏览器直连后端 `8080` 容易引发预检 OPTIONS 拦截及 Cookie/Token 策略差异。
- **解决方案**：
  - 统一 Axios `baseURL` 为 `/api`；
  - 依托 Vite 本地服务器的 `server.proxy` 配置，实现平滑代理转发至 `http://127.0.0.1:8080`，彻底消除跨域障碍。

### 4. 角色权限拦截与水平越权防护
- 在 `SecurityConfig` 严格配置基于角色的访问控制（RBAC）：
  - `/api/user/login`、`/api/user/sendCode`、`/api/product/list`、`/api/category/list` 等公共资源开放访问；
  - `/api/order/**`、`/api/comment/**` 必须经过 JWT 认证；
  - `/api/admin/**`、`/api/user/list` 等仅超级管理员与运营管理员角色拥有访问授权。

---

## 四、 本次改动回退与版本状态声明

按照用户的明确指令：
> *“你现在把改动回退到我们修改完qq邮箱那，然后写好总结文档上传至远端仓库”*

平台现已执行如下操作与版本对齐：
1. **保留核心配置**：
   - 完整保留 `mall-backend/src/main/resources/application.yml` 的 QQ 邮箱 SMTP 授权码与 587 端口通信配置；
   - 完整保留用户注册/找回密码时发送验证码邮件的完整业务逻辑；
   - 完整保留前台商城现代化**主页 (`emall-client/src/views/home/index.vue`)** 的美化布局、全量多级分类、活动秒杀、公告弹窗及客服联动效果；
   - 完整保留后台管理系统的功能完善与逻辑缺陷修复。
2. **精确回退子页面**：
   - 严格回退在邮箱配置之后进行的其余 9 个客户端子页面（商品详情 `detail.vue`、购物车 `cart/index.vue`、结算页 `checkout.vue`、订单列表 `list.vue`、发表评价 `comment.vue`、个人中心 `profile.vue`、收货地址 `address.vue`、收藏夹 `favorites.vue`、我的评价 `MyComments.vue`）的代码，恢复至邮件配置完成时的稳定版本；
   - 确保原有功能与业务逻辑纯粹、可靠。
3. **构建验证**：
   - `emall-client`: `npm run build` 成功完成，0 报错（耗时约 6.9s）；
   - `emall-admin`: `npm run build` 成功完成，0 报错（耗时约 8.2s）；
   - `mall-backend`: `mvn package -DskipTests` 成功打包生成可执行 JAR（BUILD SUCCESS）。

---

## 五、 一键启动与开发部署指引

为方便项目交接与本地快速复现，根目录下配置了一键启停 PowerShell 自动化脚本：

### 1. 一键启动全栈 5 大服务
在项目根目录打开 PowerShell 执行：
```powershell
.\start-all.ps1
```
脚本将按依赖拓扑顺序自动启动：
1. **MySQL 8.4 数据库** (端口 `3306`)
2. **Redis 缓存服务端** (端口 `6379`)
3. **Spring Boot 后端服务** (端口 `8080`，自动注入 JWT 密钥与 QQ 邮箱授权码)
4. **买家前台客户端** (端口 `5173`，访问 [http://localhost:5173](http://localhost:5173))
5. **管理运营后台端** (端口 `5174`，访问 [http://localhost:5174](http://localhost:5174))

### 2. 一键安全停止
```powershell
.\stop-all.ps1
```

### 3. 默认实测账号体系
- **前台买家账号**：`test01` / `admin`（密码：`123456`）
- **后台超管账号**：`admin`（密码：`123456`）
